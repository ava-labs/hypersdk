[package]
name = "balance"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmlanche = { workspace = true }

[build-dependencies]
wasmlanche = { workspace = true, features = ["build"] }
